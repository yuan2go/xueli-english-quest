# Codex · Refoundation 06 执行提示词

以下提示词交给单一 Codex 实现负责人使用；详细设计由现有文档承担，不把本文当第二套规格。

```text
在 yuan2go/xueli-english-quest 完成 WP-GAMEPLAY-REFOUNDATION-06。不是继续给旧答题流程加动画，而是在现有应用内重做可玩的英语工具解谜主游戏。

先读取最新 origin/main、完整 SHA、git status、AGENTS.md、CLAUDE.md、README、docs/STATUS.md、docs/README.md，再详细阅读 01–05、07–12 与该工作包。对照 docs/12 和实际代码定向确认差距，清理冲突的旧设计、当前入口、失效 Prompt/引用后编码；保留历史证据、必要旧 decoder/fixture，不再重复整篇审计。

以工业级边界完成整个工作包：先从正式首页做出 R1 两种机制不同的真实解法，再完成 R2/R3、条件改变回访、词义与基础语法教学/辅助及独立练习、WordSpell、魔法工坊、词语册和真实结局。第一关没有真实选择就先改核心，不继续铺内容；但不得只交一关、类图、引擎骨架或平行 Demo。

允许重写不合适的 UI、领域、内容和存档模型；保留 React/TypeScript/Vite、一个应用与一个确定性写入链。尺寸/可达性/开合/支撑/容纳必须影响实际行为，拖动不能瞬移；目标读取世界，不检查参考解法步骤。新规则内容化，原故事 route-sheet 过路后恢复地图、主角/纸偶分离和独立野餐垫仍受保护。主角尺寸变化不改变身份，也不能复制或变成物品。

修复短句解析限制；重复词块用独立 ID；有限规则处理接受表达、指代、大小写与标点。指令执行，描述只观察；语言正确、任务满足与世界许可分开。新知识先教意义；提示/示范/独立/回访分别记录；撤销不洗掉帮助和尝试，重听/拖空/未填满不算语言错误。判题不依赖 LLM。

把真实实体移动、变形、子物品收纳、小猫动作接入正式页面；保留已确认狸花猫与纸艺风格，资产单独登记并实际引用。动画只消费提交结果，完善取消/后台/旋转/reduced-motion、音频/资源失败与触屏/键盘等价操作。版本化规则与存档，保护原档和导出，不把旧完成映射到新章节。

按 docs/08 的 G01–G12 做相关必要测试并修复真实失败：规则/语言/恢复、同内核可解见证、真实 HTTP 首页完整章节、两种解法、移动端输入和故障。使用现有 npm test、typecheck:domain、build、资源/发布检查和浏览器套件，按需安装依赖；不删断言、不注入通关状态、不降低审核门槛。真机/儿童/教研/部署未执行如实标记，不因外部审核或缺 AI 凭据停掉其余工程。

具体文件拆分、布局、关卡细节和动画方案自主实现，不扩建账号/支付/社交/经营战斗/后台/实时 AI，不因更名换栈。独立分支分阶段提交、推送并创建 PR，不自动合并/强推或覆盖无关修改。回写 STATUS、工作包、设计说明和实际 AI 记录，交付 baseline/final full SHA、行为变化、必要测试/退出码、可解轨迹、浏览器证据和限制；工程、视觉、真机、教研、趣味、公开发布分别给结论。
```
