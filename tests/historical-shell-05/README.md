# Historical SHELL-05 browser contracts

These files retain every original assertion for the retired page; they are not selected by the current Playwright testDir. The original page is reproducible at baseline `74ea7358ce41738d16bfa5dab29e94f581605a5c`.

Refoundation intentionally replaces the old main story UI. Current `tests/browser/refoundation*.spec.mjs` validates both R1 mechanisms, the complete R1/R2/R3 chapter, request/observation, workshop and word book, changed revisit, scoped input and fault recovery from `/`. Historical story identity/crossing/decoder and all 46 original Node regressions continue running under `npm test`. This move does not remove assertions to mask a failure of the current page.
