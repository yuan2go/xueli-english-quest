# WP-PLAYFUL-GAME-03 · 主线打磨、魔法野餐与趣味重玩

> **Historical delivery / 历史交付**：本包保留原版本范围和证据，不再授权新开发。固定挑战/步骤、旧页面与旧 UI 接入点均非当前产品规范。现行规则见 01–05、11/12，当前包为 [WP-WEB-GAME-SHELL-05](WP-WEB-GAME-SHELL-05.md)。

2026-09-17。本包暂停 AI/工坊/后台；不增加词表、主线步骤、强制练习或养成。实际基线 `0ff79584a96da53e512a6702c7987d4ab65e1d20`，独立工作树 `xueli-wordspell-playful-game-03`，分支 `feat/wp-playful-game-03`；保护原树与设计分支。本地增量：`cfb2139`（领域、模式命令、指令与资源修复），`20775514e6b1dd347e0e639c93640f10349aac72`（完整游戏 UI/回归）。最终 SHA 见交付回复；不 push、合并或部署。

| 范围 | 实际行为及入口 |
| --- | --- |
| 主线手感 | 同实例前后形态投影；关键变形/过路有界、可跳过，普通结果短反馈；真实字母拖影与返回，固定种子排列，稳定过路位置；Scene 按事实显色修复 |
| 自由野餐 | 结局主要入口；拼词唤回六个已有实例、cat 不复制；真实 in/on/草地/worn；背包打开后选择内部物品、取出；帽子任选/换下/收纳；纸偶与地图可逆换字，占用拒绝并提示退路 |
| 三个玩法 | 帽子搭配：纸偶变帽、任选戴、另一顶收包；背包找物：开包、取指定物、放垫；野餐小帮手：变新座位、安置客人、收 cap，可调整顺序。均三动作目标，含完成/退出/重玩/恢复 |
| 反馈与重玩 | 真实操作触发帽檐/点头、背包探头、地图指路与垫子邀请；少量文案变体、重复缩短，不抢操作语音。稳定种子改变字母/物品排列与活动客人/找物目标；回顾来自实际事件 |
| 状态/恢复 | 同一个 domain.transition，picnic 显式模式边界，主线规则不放宽；一次位置、换帽原子化、占用/包含保护；四个独立玩法日志，不污染学习记录；确认重置备份原布置 |
| 审计修复 | 指令结构/文本/音频/来源/效果校验同用于生产；RIFF 按字节；首次不计回访；确切旧档保留帮助和音频证据迁移，未知档原样保留 |

有效规则已更新 01/02、架构 04、协议 05，不创建平行规格。稳定接口为原 entityId、WordId/assetId、受限演出 phase、ObjectButton 与语义命中区域；worn 是位置，不改词形。现有临时 SVG 明示，设计 WebP/MP3 未自动接入，未调用付费生图/模型。

测试命令、数量、体积和视口实测以 [STATUS](../STATUS.md) 本次结果为准：34 Node、8 HTTP、类型/领域/构建/开发资源通过，发布检查实际 BLOCKED。新增浏览器路径从首页手工式 DOM 操作完成主线及全部活动，没有预置通关存档。修复了实现中出现的日志 undefined 序列化恢复差异、支撑物选择歧义、移动端活动按钮可访问名称及截图中的地图文字遮挡；原断言保留，旧固定演出时长断言改为普通短反馈＋关键变形阶段验证。

代码与功能自动化已完成。正式美术/录音/教研 PENDING；真机/听审/儿童试玩 NOT_RUN；公开发布 NOT_READY。AI/Provider 验证不在本包范围，无调用。当前证明操作与状态一致性，不声称已证明儿童趣味性或词汇掌握。未发现仍阻断本包主线/野餐/活动的已知功能问题；素材、真实触屏手感和教学效果仍需真实验收。
