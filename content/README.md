# 内容目录

当前内容权威为 `src/content/adventure.ts`、`sentences.ts`；运行合同见 docs/05。

`drafts/picnic-world-plan.json` 为 **historical fixture**，仅保留给 `tests/world-plan.test.mjs` 验证旧领域正确路径及漏铺路反例。固定十二挑战/十三步骤不是当前设计，也不代表 UI 通关或教研审核。旧 content/story、session/save 等是精确历史日志兼容链，禁止按旧 step 伪造新目标。
