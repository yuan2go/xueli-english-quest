# Codex 开发启动提示词

```text
在当前 xueli-wordspell 仓库执行 WP-01（docs/work-packages/WP-01.md）。先读取 AGENTS.md、README.md、docs/STATUS.md 及工作包直接引用的有效规格，核对当前 main、分支与工作树；保留无关修改。若 WP-01 已完成则验证实际结果，按路线图进入尚未完成的下一包，不重复重建。

目标是把开发验证骨架变成手机可玩的 map→mat→铺路→map 闭环，完成工作包所有必要交互、领域/步骤守卫、音频失败路径、存档与必要回归。采用简洁且可靠的工程实现；既不只写计划，也不扩成平台。优先完整可用的纵向功能，具体组件与细节自行判断。

领域状态为权威，动画不决定正确性；两个工具共用同一引擎与合约。缺少模型凭据不阻断本包。素材/语音未经审核如实标记，禁止伪造真实调用、教研、真机或测试通过。

只运行与风险相关的必要测试，修复发现的问题；联网后生成真实依赖锁并验证类型和构建。更新 STATUS，报告代码基线/最终 SHA、实际完成能力、测试命令与结果、未运行项和下一包。若发现另一个 agent 正在实现同包，改为独立审查或领取明确不冲突的范围，不覆盖其工作。
```
