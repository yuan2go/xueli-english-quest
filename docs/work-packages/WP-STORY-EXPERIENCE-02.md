# WP-STORY-EXPERIENCE-02 · 原方案主线体验闭环

> **Historical delivery / 历史交付**：本包保留原版本范围和证据，不再授权新开发。固定挑战/步骤、旧页面与旧 UI 接入点均非当前产品规范。现行规则见 01–05、11/12，当前包为 [WP-WEB-GAME-SHELL-05](WP-WEB-GAME-SHELL-05.md)。

> 集成状态补注（2026-09-17，WP-PLAYFUL-GAME-03 核验）：下述为本包原交付证据；提交 `28231ec765c4f8d5026d8b63bfe26aafd2c996b0` 及实现现已在 origin/main `0ff79584a96da53e512a6702c7987d4ab65e1d20` 的祖先链。原“本地未合并”仅描述当时状态；本次验证见 STATUS，不改写历史 PASS 的适用版本。

2026-09-17，本地实施与自动化验证完成；正式资源、教研、真机和发布分别待验收。基线 `6b06c9efa149ea3643b3957d98a349b845b9c7fb`，独立分支 `feat/wp-story-experience-02`。接续 WP-PLAYABLE-STORY-01，未扩展词表、关卡、平台或 AI 工坊。

可审查本地增量：`ffb256ca69f9af561636eccdf1ca0562d779f545`（协议/资源/会话证据）、`0c3a46a96402ca42217cf6b030f09a26fe7bc78e`（交互和体验）、`5ff8551157ec3f1246d0e4c43205c0794a0a2b6c`（反馈分阶段与首帧修正）。随后仅整理交接和截图；最终仓库 SHA 见交付回复。

## 覆盖与实现入口

| 本包要求 | 实际实现 |
| --- | --- |
| 故事因果与结果调度 | `content/feedback.ts` 的十三项受限定义；App 绑定成功事件、分阶段表现，最长 3.6 秒，操作门控、跳过/暂停/后台/刷新稳定恢复；同一音频服务先结果词再下一任务 |
| 场景直接摆物 | `game/interaction.ts` 的世界语义目标，`ui/Scene.tsx` 整个物品/湿墨区域命中，点击/拖放共用 submit；保留 source/target/relation 首错解释 |
| 标志性误会 | s03 mat 投影＋小猫休息，重复缩短，草稿保留，无 World spawn；未收录组合与已知非目标词分别反馈 |
| 实际教程与帮助 | Letters 操作驱动教程、摆物即时说明、错误类型提示、Experience 中逐帧示范；示范不提交，暴露证据恢复后保留 |
| 绘本与两类结局入口 | 成功事件派生局部修复和三页完成；儿童野餐/词卡重听/确认重开，成人独立摘要/明细/导出/清除 |
| 资源/音频合同 | manifest、resource-contract、check-resources；图片多格式、录音加载校验/失败/超时/重试/取消；observe 绑定任务和资源版本，正式/开发来源分开 |
| 可操作性与版本恢复 | Step 字母多重集、editable/source 当前词形、场景可选实体/可达目标、效果/帮助/资源/反馈校验，复用生产 run；pack 3 / schema 2，不兼容原档保留并可导出 |

原 `domain/world.ts` 未改：route-sheet 同实例、crossed-ink 边界、不可变同伴、独立 cat-card/picnic-mat、重复/循环/占用支撑物保护保留。没有后台、密钥、模型调用、第三方遥测或付费资源。

## 设计接续与文档冲突

读取了实际主分支的源码/文档。`origin/design/art-recovery-20260917` 有独立 WebP 和两段 MP3 导入证据，但证据明确不等于美术/教学审核；没有合并、替换或改动该分支。本包继续使用明确标注的临时 SVG 和开发 TTS。正式接入点是稳定的 entityId、manifest assetId、`result-sXX` 反馈 ID、自然区域 `bag-main:in` / `picnic-mat:on` / `ink-road:across`。

修正 04/05 中残留的“初始化/后续模块”、快照存档与 schema 1 描述，以已实现的单一命令日志为准。玩法细节补入 02，不另建平行规格。反馈中文文本属于待审核创作文案，不伪称有旁白录音；现有音频注册六词四句。

## 证据、发现和修复

命令结果及范围见 [STATUS](../STATUS.md)，本次截图与 UI 自检见 [证据](../evidence/story-experience/README.md)。29 项 Node 测试、7 项真实 HTTP 浏览器测试；没有预置通关状态，手机由正常入口走完十三步。模拟触控不等于真机。录音适配器单测使用明确标记的边界 fixture，不代表真人录音听审或浏览器解码全部格式验收。

本包修复：演出后台切换可能播放下一题；终态/旧音频回调误更新；音频未播放仍被归类为独立；不自然的关系按钮；缺目标字母却能由 correctInput 通过的内容漏洞；错误投影/教程仅文字；儿童出口直达日志；结局修复清单过长/对齐分散。录音测试最初用固定两轮 event loop 等待异步 SHA，暴露测试时序问题，改为等待可观察终态并清理服务；未删断言或跳过失败。未发现需要夹带修复的无关领域问题。

存档升级不猜测迁移：schema 1/pack 2 与新音频证据口径不兼容，原始两份数据可导出，重开需确认并备份。新包内刷新不会丢失帮助分类或 s04a/s04b 边界。

## 交接与后续最小条件

本次仅本地增量提交；没有 push、PR、合并、部署。历史浏览器 CI 补丁保留，可由具备 workflow 权限者应用；本包远端 CI NOT_RUN。无需扩大凭据。首页与设计预览启动命令见 STATUS。

后续 AI 工坊真正缺少的是：受保护的草稿请求/取消接口、受限词表及素材版本输入、带 provenance 的不可信 Step 草稿输出、与生产 run/letterLayout/sceneTargets 共用的有限草稿可操作性验证、人工审核和显式版本发布。当前 validateStory 明确只接受这条十三步主线，不能宣称已支持任意工坊草稿。先为 2–4 步草稿定义有限入口世界和允许结局，再复用同一判题与物品规则；不放宽规则另造模拟器。在线生成仍需授权凭据/预算，生成器不接触儿童实时判题或成绩，主线保持离线于模型。

正式插画/录音/教研、iOS/Android/iPad 真机、目标儿童试玩、公开 HTTPS 与公开发布：均未由本包工程测试替代。
