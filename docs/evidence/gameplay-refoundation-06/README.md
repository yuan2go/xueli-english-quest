# Refoundation 06 · exact-code evidence

代码 SHA `7f42995006884e3bf17ef9c61a17b3f64557ba5e`，src tree `2b9bb458e87681817521dd0c2d906b3c904b23f9`。本目录为实际工程结果；历史 SHELL-05 证据不继承为当前 PASS。最终交付只在这一源代码树上追加文档/证据。

[checks.json](checks.json) 给出命令、exit code、日志与源 SHA；[witnesses.json](witnesses.json) 给出同内核受控枚举结果和可重放轨迹。没有浏览器通关状态注入。UI 通过 HTTP `/`，故障用例仅注入所声明的资源/存储/音频/旧档故障。

| 合同 | 实际覆盖 |
| --- | --- |
| G01 | quest.test R1 支撑/洞口、check:levels 两机制 witness；refoundation.spec 正常首页实际走两解 |
| G02–G04 | 容纳/承载/净空、不可达把手/越墙、整树搬运、占用/循环/配额/角色身份；原 46 个旧回归持续执行 |
| G05 | Open 短句、有限变体、重复 the、it/同名歧义、描述只读、language/task/world 分开；UI 保留 blocked 草稿 |
| G06 | malformed/stale/重复 attempt；真实 CDP touch cancel/多指、lostcapture/Escape、旋转；点击/键盘/词块拖入重排退回；动画中撤销与刷新 |
| G07 | 撤销保留帮助/尝试，历史完成与当前 goal 分开；示范后再做不变独立；独立窗口重开词义永久增加 text 支持 |
| G08 | schema/content/rules/载荷白名单、重放投影、已知 v4、未知/坏档、超限、备份失败、导出与存储失败 |
| G09 | 真实 R1→R2→R3 结局→工坊→词语册→回访→刷新；意义先行，辅助/独立与示范实际可操作 |
| G10 | 模拟五视口/旋转，reduced-motion/跳过/后台，取消音频与播放失败降级；进度不等动画 |
| G11 | 唯一 registry 的原文件/master字节/hash/尺寸/格式；[HTTP 首页资源](http-assets.json)、[完整章节资源](http-chapter.json)；审核状态不伪造 |
| G12 | 工程观察到两机制、物品太大/把手太高/洞口封闭导致不同后果与重新选择；儿童趣味性仍 NOT_RUN |

图片：[R1 支撑高度](r1-support.png)、[支撑方案完成](r1-step.png)、[穿洞方案完成](r1-hole.png)、[手机真实结局](phone-ending.png)。视口截图 `viewport-360/390/768/1024/1440.png`。每个截图为运行 DOM 和正式独立资产，非整页美术图或伪造状态。

性能：[before](performance-before.json) 基于 Git 导出的 `d45bb82ea23057ccb369d6c777c488b90e823929`，与最终相同脚本、浏览器、390×844、CPU throttle=4；[final](performance-final.json) 绑定最终代码 SHA。记录 layout/script/longtask，不能证明真机帧率或统计提升。测量用临时 Git 导出仅为复现旧版本，不是保留的第二应用。

发布检查实际 exit 1，首个阻塞为 box 未审核；没有降级审核。资源、正式语音、教研、真机、儿童、公网部署分别 PENDING / NOT_RUN。Actions 实际 enabled=false，因此没有远端 CI PASS。
