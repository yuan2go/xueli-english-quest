# Claude Code entrypoint

@AGENTS.md

共同规范仅在 AGENTS.md 维护。读取 README、STATUS、01–05、11/12 和明确分配的工作包。当前为 WP-WEB-GAME-SHELL-05，不自动启动历史 WP-01。

一包一实现负责人；另一个 agent 可独立审查，不在同一工作区另开实现。正式入口、真实 map → mat → 铺路 → map、恢复与游戏体验是验收对象；缺模型凭据不阻断确定性游戏。
