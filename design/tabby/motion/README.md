# WP06 motion assets · engineering integration, rights review PENDING

Built-in imagegen generated both native 1254 × 1254 RGBA masters on 2026-09-20. The requested size was not honoured by the tool; metadata records the actual dimensions, not the prompt size. No model/version/seed was exposed. Input images: `public/assets/game/tabby/cat-action.webp` and `bag.webp`, already in the approved character lineage; this does not confer formal rights approval.

The four-frame walk uses a 2 × 2 atlas: each cell is 627 × 627, ordered leading forepaw → passing → opposite forepaw → passing, 8 frames per second while translating. Head/ground anchors are in the single runtime manifest's `motion` field. Display maximum per cat frame is 220 CSS px. Each pose preserves the white locator on the viewer's right neck; CSS does not mirror. The backpack uses a separate open counterpart, ground/mouth anchors in the same manifest. Actual Alpha was inspected (transparent pixels counted), not inferred from a checkerboard.

`metadata.json` is generation/processing evidence, not a second runtime registry. `src/content/visual-assets.ts` remains the sole runtime resource registration. `scripts/prepare-motion-assets.mjs` makes native-size Canvas WebP derivatives (quality 0.9); no resizing, cropping, upscaling or invented frames. The public WebP files are consumed by `CharacterArt` and `Scene`, not merely stored. Original PNGs are retained for provenance and future controlled derivatives.

## Submitted prompts

Walk: Use case: identity-preserve. Asset type: production game walk animation spritesheet, transparent RGBA, exactly 2 columns by 2 rows of equal square cells on a 1536x1536 canvas (each cell 768x768). Input image is the approved brown-grey tabby cat character. Create a four-frame gentle walking cycle, frame order left-to-right top-to-bottom: left forepaw leading, passing pose, right forepaw leading, passing pose. Preserve the exact character rendering style and identity, gold eyes, pale muzzle, gray collar, green scarf, olive backpack. The white round tracking device MUST stay on the viewer's RIGHT side of the neck in EVERY frame, fully visible. Never mirror. Three-quarter facing viewer-right exactly like input. Keep head size and head center identical across frames, paws use the same ground baseline, cat centered at same scale in each cell. Animate paws and slight body weight only, preserve upper silhouette and ears aligned for a separate wearable hat. Tail gently articulates. Native detailed watercolor illustration like the input. Four separate whole-body cats, one per equal cell, generous transparent margins, no overlap, no labels, no ground, no shadows outside the paws, no text, no checkerboard background. Deliver real transparent background.

Bag: Use case: precise-object-edit. Production transparent game prop. Preserve this exact olive green watercolor backpack, the brown leather straps, paw patch, rolled blanket, view angle, lighting, painted style and centered size. Change ONLY the main top flap: open it upward behind the backpack and release its two buckles so the inside dark cloth mouth is clearly visible as an empty container into which objects can be placed. Keep the rest of backpack body and position unchanged, no contents, no characters, no text. Genuine transparent RGBA background, square 1024x1024 native detailed asset, whole backpack visible with generous transparent margins matching input, bottom baseline at 88% image height. This is the open state counterpart to the supplied closed backpack, not a new design.

## Limits

Four authored frames provide a short stepping cycle, not free locomotion or a skeletal actor. Existing idle/thinking/action/happy poses remain; blink is a local eye overlay, and hats follow the actor anchor. Alignment/appearance has engineering sampling, while art direction/rights review and real-device perception remain NOT_RUN. Generated backpack proportions differ slightly from the closed reference; the same anchor and short opening transition are used. Formal asset approval remains PENDING.
