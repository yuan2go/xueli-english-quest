# Claude Code entrypoint

@AGENTS.md

共同规范只维护在 AGENTS.md，避免两份规则漂移。开始前阅读 docs/STATUS.md 与明确分配的工作包。

未指定工作包时，从 docs/work-packages/WP-01.md 开始；若该包已有其他 agent 作为实现负责人，只做独立审查或认领 docs/09-roadmap-and-agent-handoff.md 中不冲突的任务。不要在同一工作区启动另一套实现。

首要验收对象是手机可玩的 map → mat → 铺路 → map，不是文档数量。缺少模型密钥不阻断主线开发。
