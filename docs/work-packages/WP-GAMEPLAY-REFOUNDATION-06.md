# WP-GAMEPLAY-REFOUNDATION-06 · 英语工具解谜主玩法重构

状态：READY_FOR_IMPLEMENTATION；运行实现 NOT_STARTED。本文件是当前唯一工作包，按阶段交付一个完整新章节，不要求一次建设完整商业平台。代码事实基线 `a9a62e3eb632781595b40171dfb442bf0fbbe55e`；执行时先读取包含本设计的最新 origin/main，不强行回退到该 SHA。

## 目标

在现有雪梨英语奇旅中，以“观察—选择英语工具—世界改变—调整解法”为核心，完成新主章节、教学/练习工坊、词语册、回访、因果动画与恢复。允许重写不适合的 UI、内容和核心模型，但仍只有一个应用、一个正式入口和一个确定性世界写入链。

完整定义见 [01](../01-product-and-scope.md)、[02](../02-gameplay-and-levels.md)、[11](../11-web-game-product-blueprint.md)；架构/协议/代码映射见 [04](../04-architecture.md)、[05](../05-content-and-runtime-contracts.md)、[12](../12-web-game-technical-design.md)；验收见 [08](../08-test-release-and-operations.md)。不要再复制平行蓝图。

## 开工前

读取最新 AGENTS/CLAUDE、README、STATUS、上述合同及 03/07/09/10；核对本地状态、main full SHA、相关未合并工作。逐项确认 12 的差距是否仍存在，不重复全仓审计。清理仍然引用 SHELL-05 作为当前目标的 Prompt、内容导航、提交说明或过期设计；有历史价值的记录保留并标 historical。先完成此定向治理，再修改运行代码。

## A · 一个真正成立的核心关卡

重构本包所需的实体属性、有限空间、通道/把手、容器/支撑与角色行动。把旧六词和物品特判限制到旧规则集，保护原故事不变量。采用有限规则与内容声明，不先建 ECS、物理引擎或通用编辑器。

从正常首页直接进入 R1：工具有意义演示；孩子能通过“箱子作为台阶”和“主角缩小穿洞”两种不同机制取回篮子。开门、移动、搬运、尺寸与净空/高度须实际计算；合法其他方案照常接受。至少展示一个受阻原因和可撤销恢复，不能用 scripted success flags 或隐藏 Demo 替代。

同步实现本关需要的短句解析、指令/描述分离、帮助和版本化保存基础。领域见证和真实 HTTP 操作都要成立。若两种方案只是动作换序或英语只是装饰，先改设计再继续扩关。

## B · 完整内容与学习闭环

完成 02 规定的 R2、R3、一个改变条件的回访，复用同一规则而非复制页面。补齐词义学习、WordSpell、基础句型教学、辅助/独立练习、听音请求与观察描述。所有新词句/资源登记并保持真实审核状态，不把候选词表当已教研通过。

实现魔法工坊和词语册的实际入口/内容/返回；工坊世界与章节隔离，回访 variant/seed 可恢复。结局来自真实世界，当前目标与历史完成分开；不新增经营/战斗/货币/账号/在线 AI。A 通过后继续 B/C，不以一关原型结束整包。

## C · 完成度、可靠性与表现

实现稳定实体的移动/变大缩小/开关/收纳与携带子物品动画，小猫的观察/行走/操作/受阻/庆祝连续可信。保留确认的狸花猫形象，复用适合资产，缺失新物品原创制作并单独登记；不使用无授权拼贴。文字层独立于图像，资源入库和页面引用分别核验。

完善点击/拖动/键盘、取消、多指、旋转、焦点、工具滚动、后台、音频取消/失败、reduced-motion。业务提交不等动画，旧 cue 不覆盖新 revision。记录同条件性能观察并修复真实卡顿，不预报提升。

完成撤销/重开/会话隔离、帮助不可洗白、新旧 codec/白名单/导出与备份、损坏/未来/存储失败。新规则显式版本化，不自动迁移旧成绩；旧世界与存档高价值回归保留。

## 必要测试与完成条件

落实 08 的 G01–G12，不按测试数量考核。核心至少包括：尺寸/可达性/关系/身份、原子性和重复命令、短句/重复词块/多表达/指代/blocked、撤销保留支持、版本化重放和旧档、R1 两种规则见证、正式首页完整章节与各表面往返、关键触屏/刷新/故障路径。

使用现有 npm test、typecheck:domain、build（包含 typecheck）、check:resources、check:release、test:browser；环境或依赖需要时运行 npm ci。新增有界关卡校验脚本时实际实现/登记后运行；预算耗尽为 UNKNOWN，不报 PASS。不得注入通关状态、删除失败断言、降级审核门槛或堆无意义快照。

完成需有真实代码与 HTTP 浏览器证据，不以文档/类图/素材文件数量交付。实体设备、目标儿童、教研/录音/权利和公网部署独立：不可用时标 NOT_RUN/PENDING/BLOCKED，继续能独立完成的工程。

## 权限、交付与状态

Codex 为本包唯一实现负责人；其他 agent 只审查或承担明确不重叠工作。默认独立分支/worktree，阶段提交、推送和 PR，不自动合并 main、不强推、不覆盖无关改动。实际部署先核对已有站点与授权，不新建第二个站点冒充更新。

维护 STATUS、本包结果和对应合同；报告 baseline/final full SHA、分支/PR、正式入口变化、代码与资源映射、测试命令/退出码/环境、可解 witness、浏览器路径/截图、性能观察、未完成/限制。提交设计说明和实际 AI 使用记录，不写虚构教学/儿童或 Provider 结果。

独立给出 ENGINEERING_READY、VISUAL_COMPLETE、DEVICE_VALIDATED、TEACHING_REVIEWED、FUN_VALIDATED、PUBLIC_RELEASE_READY，默认均 NO；有对应证据才改变。工程可玩不等于公开发布或教学效果。

## 实际执行记录

尚未开始代码实施。当前交付是需求与技术合同；本节由实现负责人填写真实里程碑、失败修复、SHA 和证据，不预勾完成。

### A 验证记录

2026-09-20：正式 `/` 已切到 Refoundation；R1 两种机制（大箱支撑高把手 / 同猫缩小穿洞开内把手）通过同一 transition、世界目标和 HTTP 首页验证。越墙搬篮子拒绝且不改世界，刷新可恢复。50 个 Node 测试、build、R1 浏览器通过（exit 0）。这是 A 里程碑，不是整包或视觉完成声明；继续 B/C。新增 SVG 的 master 检查按实际格式验证，旧 PNG 断言保留；旧语法 decoder 保持其版本边界。

### B 验证记录

三关、封洞回访、学习工坊/词语册已接入正式页面。R2 支持缩小苹果/改变容器/制作另一箱子；大容器带水果被窄拱门拒绝。R3 手工两种英语请求关联实际 in/on 目标，描述不改世界；工坊返回保留原章节。54 Node 回归通过（exit 0）。`check:levels -- --write` 六个有界枚举均找到见证，R1 支撑与穿洞机制分开，结果含规则版本、预算和轨迹。继续 C 的输入/视觉/恢复/平台回归与交付证据。
