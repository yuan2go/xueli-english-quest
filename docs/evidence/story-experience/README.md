# WP-STORY-EXPERIENCE-02 · 实际验证证据

日期：2026-09-17；基线 `6b06c9efa149ea3643b3957d98a349b845b9c7fb`。macOS arm64，Node 26.3.1/npm 11.16.0，Playwright 1.63.0/本机 Chromium。页面来自 `npm run build` 后实际 HTTP 预览 `127.0.0.1:4174`。没有真机或儿童参与。

已验证代码 SHA `5ff8551157ec3f1246d0e4c43205c0794a0a2b6c`；完整浏览器套件 7/7 后，最终首帧阶段重置修正另跑 `--grep 'result timing'` 1/1（13.2 秒，含服务器启动）。没有因失败而筛掉用例。

- [手机场景直接放置](phone-placement.png)：390×844，背包内部和垫子表面均为可点击/可拖到的场景区域；无额外 in/on 答案按钮。
- [手机换字结果](phone-morph-result.png)：同一 route-sheet 变形后的已提交世界、词尾对比、目标词与用途，仍须亲手铺路。
- [手机儿童结局](phone-ending.png)：真实包含/支撑关系、三页事实派生修复、词卡重听与次级成人入口。
- [Pad 核心铺路](pad-crossing.png)：1024×768，双栏真实任务与场景。
- [桌面核心铺路](desktop-crossing.png)：1440×1000。其余小屏/旋转检查不新增长期截图。

## 覆盖与证据边界

`npm test` 29/29，0 skipped；`npm run typecheck`、`npm run typecheck:domain`、`npm run build`、`npm run check:resources` 通过。`npm run test:browser` 7/7（单 worker），正常入口手机十三步与其他代表性视口核心切片、CDP 模拟触控字母交换/铺路/帽子摆放、键盘与弹窗焦点、错误/帮助/示范、两处铺路刷新、暂停/静音、结局/摘要/确认重开、资源/语音/存储失败均覆盖。

新增浏览器用例故意不触发语音结束回调：有限演出/音频看门狗仍释放下一任务；pagehide 暂停后稳定恢复；s03 同错重复缩短、示范可观察但不代做，刷新保留暴露证据；s04a 演出中刷新必须继续铺路，s04b 刷新只提交一次。录音适配器单测验证字节/哈希错误、play 拒绝后重试、取消旧回调、加载超时与静音；fixture 是边界测试数据，不是正式教学音频。

完整主线用例收集 pageerror、console error、HTTP ≥400，结果为空；故障注入用例故意失败的请求不混入正常指标。视口横向溢出检查通过，字母命中宽度≥56 CSS px。文本/图像尺寸通过真实截图复核。正文墨绿 `#254b3d` 配暖纸、辅助文字 `#636653`；错误/帮助用文案和结构表达，不只颜色。未做全设备矩阵、像素快照基线或教学效果统计。

按 sRGB 相对亮度实算，以上两种正文色对纸色 `#f5efdf` 分别为 8.50:1、5.14:1；这是指定色对检查，不是对全部控件或辅助技术兼容性的笼统认证。

## Anti-AI-Generic Review（工程自检）

玩法归属：字母印块、同一纸张变形、湿墨目标、背包收纳和野餐状态可直接操作；不是题库卡片/后台模板。装饰：纸张、折页、小径用于故事方向与场景辨识，无霓虹粒子或全屏庆祝。层级：当前任务与物品优先，修复页在顶部可展开，成人日志为次级入口。素材：全部沿用同一批已标记临时 SVG，未混入独立设计分支的未审核成果，也未冒称 GPT Image 成品。

截图发现结局修复清单过长、居中列表的圆点与文字分散；已改为按页紧凑排列并重新截图。试戴道具锚定小猫实体，避免宽窄屏飘移；换字先显示变化位置，再目标词和场景用途。已检查截图中裁切、文字溢出和操作遮挡。这里是功能 UI 自检，不是用户验收或正式美术验收。

## 发布与未运行

`npm run check:release` 实际退出 1：未审核 `cat` 图像首先被拒绝；所有正式美术/录音和内容审核仍待完成。该结果记 BLOCKED，不计发布 PASS。旧 workflow 权限缺口的补丁仍在 `../playable-story/browser-ci.patch`；本包不 push、不扩大权限，远端 CI NOT_RUN。

真机、浏览器实际录音多格式听审、教学有效性、儿童试玩、公开网络/微信/首次离线、正式资产授权及发布：NOT_RUN / NOT_READY。无在线模型或收费资源调用。

最终构建：JS 250.49 kB / gzip 81.33 kB；CSS 19.66 kB / gzip 5.50 kB；HTML 0.58 kB / gzip 0.43 kB。以 Vite 实际输出的十进制 kB 为准，包含 React；不换算成网络首开时间。
